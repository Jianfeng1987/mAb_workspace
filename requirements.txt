# Python 依赖（核心分析 + Web 框架）
biopython>=1.81
openpyxl>=3.1.2
pandas>=2.0.0
numpy>=1.26.0
anarci>=1.3
abnumber>=0.1.6
cos-python-sdk-v5>=1.9.25
streamlit>=1.28.0
matplotlib>=3.8.0

# 系统依赖（Ubuntu/Debian 需额外安装，非 pip 包）
# 安装命令：sudo apt update && sudo apt install -y hmmer muscle